.DS_Store
Thumbs.db
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
public/
dist/
build/
.env
.env.*

# transient recall-probe labeling worksheet (regenerated each run)
eval/recall_probe.json

# transient candidate-backlog review worksheet (regenerated each run)
eval/candidate_triage.json

# transient vocabulary re-check worksheet (regenerated each run)
eval/candidate_refilter.json

# transient search-allowlist health worksheet (regenerated each run)
eval/domain_audit.json

# transient working dir for the issue-form report importer (downloaded PDF, plaintext, manifest)
.ingest_work/


# The agent lane's compiled bytecode; its run logs (agents/runs/) are the audit
# trail and ARE committed on purpose — see docs/gegenevidenz-lane.md.
agents/__pycache__/
